# Copyrighted recordings — never committed (anywhere): source video + mastered audio
*.webm
*.mp3
# Intermediate extracted frames (regenerable via tools/extract_segments.py)
frames/
# Build artifacts (regenerable via build_demo.sh)
src/*.prg
src/*.sym
src/*.efo
src/*.bin
# Generated source — only src/lyriceng.asm is hand-written; the rest are
# emitted per active clip by gen_parts.py / lyric_assets.py.
src/p*.asm
src/lyric_n.asm
src/lyric_fade.asm
src/test_core.asm
parts_pef/
out/
__pycache__/
cand/
# Active-clip working symlinks at repo root (authoritative copies in clips/<name>/)
/clip.json
/segments.json
/lyrics.json
/picks.json
/koala
/*.sid
/*.sng
/*.lrc
/*.mid
# Generated per active clip (gen_parts.py rewrites these)
/build_demo.sh
/script_demo
